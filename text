1.在引入Java包mocha时，其依赖jspecify是用Gradle构建的。引包构建环境是Maven且离线的产生报错。
- 在预检时构建工具无法通提供则直接打回，不符时看能不能替代或是向下兼容，否则也打回。
2.在预检的联网阶段会将构建工具和依赖一样匹配升级，但实际不许。
- 用构建白名单解决，将所有已有构建工具写成白名单，ai拿着这份白名单去过滤。
3.在构建benifits时ai出现跳步bug，分析是skill对“缺包”状态定义不清晰导致未写入构建引入包引入
- 对该步骤扩写，调用脚本重新判断缺包，确保ai不会因定义不清导致错误判断跳步。
4.完成“计算人人懂开源”课程的学习并完成相应的考试。
